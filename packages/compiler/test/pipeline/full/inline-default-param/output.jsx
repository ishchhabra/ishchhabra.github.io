const a = function a(a = 5) {
  return d.slice(0, a).map((a) => ({
    slug: a.slug,
  }));
};
const d = [];
export const Home = function Home() {
  const [$50_0 = 5] = [];
  const b = d.slice(0, $50_0).map((a) => ({
    slug: a.slug,
  }));
  return (
    <div>
      {b.map((a) => (
        <span key={a.slug}>{a.slug}</span>
      ))}
    </div>
  );
};
